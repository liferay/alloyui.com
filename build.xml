<?xml version="1.0" encoding="UTF-8"?>
<project name="alloyui.com" default="compile" basedir=".">

    <property name="root" value="." />

    <target name="install">
      <echo>Installing DocPad</echo>
      <exec executable="docpad" dir="${root}">
        <arg value="install" />
      </exec>
    </target>

    <target name="generate">
      <echo>Generating DocPad</echo>
      <exec executable="docpad" dir="${root}">
        <arg value="generate --env static" />
      </exec>
    </target>

    <target name="run">

      <if>
        <not>
          <available file="${root}/node_modules" />
        </not>
        <then>
          <exec executable="docpad" dir="${root}">
            <arg value="install" />
          </exec>
        </then>
      </if>

      <echo>Running DocPad</echo>
      <exec executable="docpad" dir="${root}">
        <arg value="run" />
      </exec>

    </target>

    <target name="clean">
      <delete dir="node_modules" />
      <delete dir="out" />
    </target>

</project>